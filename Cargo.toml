[workspace]
members = [
  "crates/wiki-model",
  "crates/wiki-index",
  "crates/wiki-knowledge",
  "crates/wiki-runtime",
]
resolver = "2"

[workspace.package]
license = "GPL-3.0-or-later"

[profile.release]
opt-level = "s"
lto = "thin"
codegen-units = 1
strip = "symbols"
panic = "abort"
