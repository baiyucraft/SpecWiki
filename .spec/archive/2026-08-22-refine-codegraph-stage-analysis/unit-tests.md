# Unit Tests

- UT-01 (ST-01): runner defaults to detection-only and returns no side-effect calls.
- UT-02 (ST-02/03): force/skip modes select external orchestration safely.
- UT-03 (ST-04): existing index short-circuits install and project init.
- UT-04 (ST-05): bilingual Skill/template contracts enforce ordered analysis and structured evidence.
- UT-05 (ST-06): package file manifest excludes `.codegraph` runtime data.
