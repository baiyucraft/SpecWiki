[package]
name = "wiki-model"
version = "0.1.0"
edition = "2021"

license.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
serde = { version = "1.0", features = [ "derive" ] }
blake3 = "1.8"

[dev-dependencies]
serde_json = "1.0"
